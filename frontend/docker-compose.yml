services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: berliners-papermatch:latest
    container_name: berliners-papermatch
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
